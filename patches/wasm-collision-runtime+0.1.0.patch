diff --git a/node_modules/scratch-render/src/RenderWebGL.js b/node_modules/scratch-render/src/RenderWebGL.js
--- a/node_modules/scratch-render/src/RenderWebGL.js
+++ b/node_modules/scratch-render/src/RenderWebGL.js
@@ -1063,6 +1063,16 @@ class RenderWebGL extends EventEmitter {
     isTouchingColor (drawableID, color3b, mask3b) {
         const candidates = this._candidatesTouching(drawableID, this._visibleDrawList);

+        // TurboWasm: optional WASM SIMD acceleration. The host application
+        // (TurboWasm Viewer) installs a hook on `this._twWasmIsTouchingColor`
+        // at runtime; when present we delegate and return its boolean result.
+        // Returning null/undefined (or throwing) falls through to the
+        // original JS path so the JS baseline is preserved bit-for-bit
+        // whenever the WASM tier is unavailable.
+        if (typeof this._twWasmIsTouchingColor === 'function') {
+            try { const r = this._twWasmIsTouchingColor(this, drawableID, color3b, mask3b); if (r !== null && r !== undefined) return !!r; } catch (e) {}
+        }
+
         let bounds;
         if (colorMatches(color3b, this._backgroundColor3b, 0)) {
             // If the color we're checking for is the background color, don't confine the check to
@@ -1265,9 +1275,14 @@ class RenderWebGL extends EventEmitter {
     isTouchingDrawables (drawableID, candidateIDs = this._drawList) {
         // if we are invisible we don't touch anything.
         if (!this._allDrawables[drawableID]._visible) {
             return false;
         }

+        // TurboWasm: optional WASM SIMD acceleration. See isTouchingColor above.
+        if (typeof this._twWasmIsTouchingDrawables === 'function') {
+            try { const r = this._twWasmIsTouchingDrawables(this, drawableID, candidateIDs); if (r !== null && r !== undefined) return !!r; } catch (e) {}
+        }
+
         const candidates = this._candidatesTouching(drawableID,
             // even if passed an invisible drawable, we will NEVER touch it!
             candidateIDs.filter(id => this._allDrawables[id]._visible));
